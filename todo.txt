17.12 - колизии лэйблов, обновление, получение данных депутата, проверка скорости загрузки сразу всех и по запросу
18.12 - карточка депутата, цвета шариков, подсветка кого надо после фильтров
19.12 - подключение бульмы,вёрстка каркаса страницы, scss
20.12 - связи клонов, слайдеры-гистограмы, фильтры-пресеты
21.12 - лэйблы в две строки и вёрстка фильтров

21.12.2018

связи клонов
фильтры-пресеты
многострочные лэйблы
иерархия лобби на карточке


шевеления:
var force = d3.forceSimulation()
        .nodes(nodes.sort((a,b)=>a.cluster-b.cluster))
        .force('collide', collide)
        .force('cluster', clustering)
        .force('x',forceX.strength(0.1))
        .force('y',forceY.strength(0.1))
        .force('collide2', d3.forceCollide(d => d.r+2 ).strength(0.8).iterations(2))
        .alphaTarget(0.03)
        .on("tick", tick)

clustering 0.1
collide 0.03